[tool.pytest.ini_options]
addopts = "-ra --import-mode=importlib"
asyncio_mode = "auto"
testpaths = ["data_structures", "iterators", "files", "concurrency", "asynchronous", "networking", "typing_and_metaprogramming"]

[tool.ruff]
target-version = "py311"
line-length = 100

