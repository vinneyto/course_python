# Async fan-in

Реализуйте async-генератор `merge(sources)`, объединяющий async iterable по
готовности элементов. Быстрый источник не должен ждать медленный. При закрытии
генератора отмените созданные задачи; исключение producer должно попасть к
consumer, остальные задачи — завершиться.

